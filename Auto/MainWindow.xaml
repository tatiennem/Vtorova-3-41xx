<Window x:Class="Auto.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Auto.ViewModels"
        xmlns:views="clr-namespace:Auto.Views"
        mc:Ignorable="d"
        Title="Toyota Manager"
        Height="1050"
        Width="1510"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        ResizeMode="CanResize"
        Background="#0E1E2E">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:CatalogViewModel}">
            <views:CatalogView />
        </DataTemplate>
       
        
        
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="337*"/>
            <RowDefinition Height="15*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4.5" />
            <ColumnDefinition Width="235.5"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#0E1E2E" Grid.Column="0" Padding="16" Grid.RowSpan="2" Grid.ColumnSpan="2" >
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 20">
                    <Border Width="50" Height="38" CornerRadius="12" Background="#1F3B57" VerticalAlignment="Center">
                        <TextBlock Text="T" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    </Border>
                    <StackPanel Margin="10 0 0 0">
                        <TextBlock Text="Автосалон Toyota" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
                        <TextBlock Text="Менеджер" Foreground="#9BB4D1" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <ItemsControl ItemsSource="{Binding NavigationItems}" Width="220">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource NavButton}"
                                    Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                                    <TextBlock Text="{Binding Icon}" FontFamily="Segoe MDL2 Assets" FontSize="18" Margin="0,0,12,0" />
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Description}" Foreground="#9BB4D1" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>

        <Border Grid.Column="2" Background="#F5F7FA" Padding="20" Grid.RowSpan="2">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>