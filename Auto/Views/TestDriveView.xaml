<UserControl x:Class="Auto.Views.TestDriveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.1*"/>
            <ColumnDefinition Width="1.1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Запись на тест-драйв" FontSize="22" FontWeight="SemiBold" Foreground="#0E1E2E" Grid.ColumnSpan="2" Margin="0 0 0 10"/>

        <Border Grid.Row="1" Grid.Column="0" Background="White" Padding="14" CornerRadius="12" BorderBrush="#E7ECF2" BorderThickness="1" Margin="0 0 12 0">
            <StackPanel>
                <TextBlock Text="Автомобиль" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Cars}" SelectedItem="{Binding SelectedCar}" Margin="0 4 0 8">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Model}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Trim}" Foreground="#5A6A7A"/>
                                <TextBlock Text="{Binding Color}" Foreground="#5A6A7A"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="Дата" FontWeight="SemiBold"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Margin="0 4 0 8"/>

                <TextBlock Text="Время (чч:мм)" FontWeight="SemiBold"/>
                <TextBox Text="{Binding SelectedTime}" Margin="0 4 0 8"/>

                <TextBlock Text="Клиент" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Customers}"
                          SelectedItem="{Binding SelectedCustomer}"
                          DisplayMemberPath="FullName"
                          Margin="0 2 0 6"/>
                <Button Content="Новый клиент" Command="{Binding NewCustomerCommand}" Padding="10 6" Margin="0 0 0 8"/>
                <TextBlock Text="ФИО" FontWeight="SemiBold"/>
                <TextBox Text="{Binding CustomerName}" Margin="0 2 0 6"/>
                <TextBlock Text="Телефон" FontWeight="SemiBold"/>
                <TextBox Text="{Binding CustomerPhone}" Margin="0 2 0 6"/>
                <TextBlock Text="Email" FontWeight="SemiBold"/>
                <TextBox Text="{Binding CustomerEmail}" Margin="0 2 0 8"/>

                <TextBlock Text="Комментарий" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Notes}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0 4 0 10"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 8 0 0">
                    <Button Content="Записать клиента" Command="{Binding BookCommand}" Padding="12 6" Background="#244566" Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Background="White" Padding="14" CornerRadius="12" BorderBrush="#E7ECF2" BorderThickness="1">
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Ближайшие тест-драйвы" FontWeight="SemiBold" FontSize="16" />
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Upcoming}" Margin="0 6 0 0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E7ECF2" BorderThickness="0 0 0 1" Padding="6 8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Slot, StringFormat='{}{0:dd.MM HH:mm}'}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Car}" />
                                    <TextBlock Text="{Binding Customer}" Foreground="#4D5A6A"/>
                                    <TextBlock Text="{Binding Phone}" Foreground="#4D5A6A"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
